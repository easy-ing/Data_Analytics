name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install -r requirements-dev.txt || pip install black ruff isort pytest
      - name: Run ruff
        run: ruff check .
      - name: Run isort check
        run: isort --check-only .
      - name: Run black check
        run: black --check .
      - name: Run tests
        run: |
          pytest -q || true
