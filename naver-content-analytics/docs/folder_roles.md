# 폴더 역할 설명

- `backend/`: 이벤트 수집 API와 간단한 인사이트 조회 API를 제공한다.
- `data/raw/`: 원천 로그 CSV(샘플 생성 데이터)를 저장한다.
- `data/processed/`: 전처리 또는 분석 산출물(예: 중간 집계 파일)을 저장한다.
- `sql/`: 스키마 정의와 KPI 분석 SQL을 관리한다.
- `etl/`: 로그 생성, DB 적재, 마트 생성 배치 스크립트를 관리한다.
- `docs/`: 아키텍처, 프로젝트 흐름, 자소서 연결 포인트 문서를 관리한다.
